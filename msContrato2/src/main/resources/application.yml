server:
  port: 8089

spring:
  application:
    name: msContrato2
  datasource:
    url: jdbc:mysql://localhost:3306/contrato_db
    username: root
    password: 
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

springdoc:
  api-docs:
    enabled: true
    version: openapi_3_0
    path: /api/v1/contratos/v3/api-docs
  swagger-ui:
    enabled: true